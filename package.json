{
  "name": "samrat-solar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && rimraf server/public && mkdir server/public && (cp -r dist/* server/public/ || xcopy /s /e dist server\\public\\)",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "rimraf": "^5.0.0",
    "vite": "^6.0.0"
  },
  "devDependencies": {}
}
